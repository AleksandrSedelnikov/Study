files=`find ~ -perm /u=x -type f 2>~/error` # в переменной files хранятся все абсолютные пути до файлов из дк и пд с правом на запуск для пользователя
tar -zcf archive.tar.gz $files # создаётся архив с файлами из переменной files