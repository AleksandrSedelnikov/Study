# создаём файл a1: nano a1, и пишем что угодно, сохраняем и закрываем nano
# создаём директорию D1: mkdir D1, переходим туда и создаём файл a3d1, nano a3d1, внутри пиши так: слово слово (на новую строчку переходишь) слово слово, и так строчек 5, желательно разные слова
cd # возвращаемся в домашнюю директорию
# создаём файл a_sort, cat ~/D1/a3d1 | sort -k2 > a_sort
mkdir D1_1 # создаём директорию D1_1
cd D1_1 # переходим в директорию D1_1
mkdir LSORT # создаём директорию LSORT
chmod 756 LSORT # выдаём права директории LSORT
cd LSORT # переходим в директорию LSORT
ln -s ~/a1 a1_symbol && ln -s ~/a_sort asort_symbol # создаём две символьные ссылки на файла a1 и a_sort
